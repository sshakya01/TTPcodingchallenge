<% include ../partials/header.ejs %>


<div id = "container">
  <div id = "newEvent">
    <a href="lists/add">Add Event</a>
  </div>

   <% for (let event of data) { %>
     <div class = "box">
       <div class = "row">
         <div class="cell1">
            <p><%= event.title %></p>
         </div>
         <div class="cell1">
            <p><%= event.status %></p>
         </div>
       </div>
       <div class = "row">
         <div class ="cell2">
            <p><%= event.category %></p>
         </div>
         <div class = "cell2">
            <p><a href='/lists/<%= event.id%>'>Info</a></p>
         </div>
         <!-- with method delete, override the post method, which is the default method for a form -->
         <div class ="cell2">
            <form action="/lists/<%= event.id %>?_method=Delete " method="Post">
               <input type="Submit" value="Delete"/>
            </form>
         </div>
         <div class = "cell2">
            <form action="/lists/<%= data.id %>?_method=PUT" method="Post">
               <input type="Submit" value="Edit" />
            </form>
         </div>
       </div>
    </div>
   <% } %>
</div>

<% include ../partials/footer.ejs %>
